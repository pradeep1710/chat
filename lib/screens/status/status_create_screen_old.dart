import 'package:flutter/material.dart';

class StatusCreateScreen extends StatelessWidget {
  const StatusCreateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Status')),
      body: const Center(child: Text('Status Create Screen - Coming Soon')),
    );
  }
}
